[gcode_macro SET_CUSTOM_RETRACTION]
gcode:
  SET_RETRACTION RETRACT_LENGTH={params.L|default(1, true)}
  SET_RETRACTION RETRACT_SPEED={params.S|default(30, true)}
  SET_RETRACTION UNRETRACT_SPEED={params.S|default(30, true)}
